# 用户明确规则

识别“以后、默认、下次、都要、不要再、永远、请记住”等明确表达为用户指定规则。该规则在用户选择保存且确认范围后立即生效；若当前环境无法写入，只在当前对话遵循，并说明未保存。

记录时必须包含：

```markdown
规则类型：用户明确指定
来源：用户原话
状态：有效
适用范围：待确认 / 当前项目 / 全局
是否经过案例验证：否
```

不得把它写成“经过多个案例验证的方法”。范围未明确时，默认当前项目，并提示用户可改为其他范围。
